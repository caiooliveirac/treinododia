# Backend

API do projeto `Treino do Dia`.

## Próximo passo sugerido
- Inicializar API com Node.js (`Fastify` ou `NestJS`).
- Configurar conexão com banco em `database/`.
- Expor endpoints de treinos (`GET /treinos`, `POST /treinos`).
