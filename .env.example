# Copie este arquivo para .env e preencha com valores reais.
# NUNCA versionar .env com segredos.
#
# ⚠️  SEGURANÇA: ao fazer pull, SEMPRE verifique se novas variáveis
# foram adicionadas e troque TODOS os valores padrão por senhas fortes.
# Nunca reutilize as senhas de exemplo em produção.

# ── Banco de dados ─────────────────────────────────
POSTGRES_DB=treinododia
POSTGRES_USER=treinododia
POSTGRES_PASSWORD=troque-por-uma-senha-forte

# Usado no ambiente local fora de containers (opcional).
DATABASE_URL=postgresql://treinododia:troque-por-uma-senha-forte@localhost:5432/treinododia?schema=public

# ── Backend ────────────────────────────────────────
PORT=3333
NODE_ENV=production

# ── Autenticação ───────────────────────────────────
ADMIN_EMAIL=admin@treinododia.local
ADMIN_PASSWORD=troque-por-uma-senha-forte
CLIENT_EMAIL=cliente@treinododia.local
CLIENT_PASSWORD=troque-por-uma-senha-forte

AUTH_SESSION_SECRET=troque-por-um-token-longo-e-aleatorio
AUTH_SESSION_TTL=8h

# ── Nginx / Proxy ─────────────────────────────────
# Porta HTTP em que o Nginx local escuta (padrão: 80)
NGINX_PORT=80

# CORS_ORIGIN deve apontar para a URL que o browser acessa.
# Com Nginx local: http://localhost (porta 80)
# Sem Nginx:       http://localhost:3000
CORS_ORIGIN=http://localhost

# ── Frontend ──────────────────────────────────────
# Base path do Next.js (subpath em que a app é servida)
NEXT_PUBLIC_BASE_PATH=/treinos

# URL base para chamadas de API feitas pelo browser.
# Com Nginx (simula prod): /treinos   (caminho relativo via proxy)
# Sem Nginx (dev direto):  http://localhost:3333
NEXT_PUBLIC_API_URL=/treinos
